import { createUseStyles } from "react-jss";

const styles = createUseStyles({
  main: {
    minWidth: 320,
    maxWidth: 1200,
  },
  title: {
    textAlign: "center",
    margin: "30px 0",
  },
});

export default styles;
