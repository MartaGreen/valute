import { createUseStyles } from "react-jss";

const styles = createUseStyles({
  main: {
    minWidth: 320,
    maxWidth: 1200,
  },
  title: {
    textAlign: "center",
  },
});

export default styles;
